NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=
OPENAI_API_KEY=
SMTP_USER=
SMTP_APP_PASSWORD=
MAIL_TO=hannau416@gmail.com
ALERT_WEBHOOK_URL=
# Optional: Cloudflare Worker URL that proxies the investing.com calendar
# fetch (see cloudflare-worker/calendar-proxy.js). Only needed in CI, where
# GitHub Actions' IP range gets blocked outright; local dev works without it.
CALENDAR_PROXY_URL=
